import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        paddingTop: 100,
    },
    stretch: {
        stretch: {
            width: 100,
            height: 100,
            resizeMode: 'stretch',
        },
    },
});

export default styles;